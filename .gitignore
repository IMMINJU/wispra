node_modules/
.env

# generated audio samples (binary). harness copies are dev-only → ignored.
harness/sample.pcm
harness/sample-ko.pcm
harness/sample-convo.pcm
# NOTE: public/sample.pcm is intentionally COMMITTED so the deployed (Vercel)
# "데모 보기" works — it's served as a static asset. Regenerate with
# `npm run sample:convo`. Keep it small (short EN↔KO conversation).

# stray temp/check files
*__chk.mjs
.vercel
